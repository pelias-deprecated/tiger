/**
 * @file The main entry point for the TIGER importer's unit-tests.
 */

'use strict';

var tape = require( 'tape' );
var createAdminValues = require( '../lib/create_record_stream' );

tape( 'createRecordStream.create(): ', function ( test ){
  test.end(  );
});

tape( 'createAdminValues.createAdminValues(): ', function ( test ){
  test.end(  );
});
